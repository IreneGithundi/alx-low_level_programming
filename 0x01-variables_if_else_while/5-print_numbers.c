#include <stdio.h>

 /**
  * main - Entry Point
  * description: Prints all numbers from 0 to 9 followed by a newline
  * Return: Always 0 (Success)
  */

int main(void)
{
	int a = 0;

	while (a <= 9)
	{
	printf("%d", a);
	a++;
	}
	printf("\n");
	return (0);
}
